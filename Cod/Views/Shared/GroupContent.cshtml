@model Cod.Models.Group

<div class="card-body">
    <div class="d-flex justify-content-between flex-row mb-2">
        <div>
            <i class="bi bi-globe"></i> @Model.Name
        </div>
        <span>@Model.CreationDate</span>
    </div>
    <div>@Model.Description</div>
</div>